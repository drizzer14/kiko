# 7-day dependency min-age: refuse versions published less than 7 days ago.
# Real npm 11 config keys (verified via context7 npm/cli docs): the value
# is in days, not minutes, and the exclude key takes a list.
min-release-age=7
min-release-age-exclude[]=Kiko
min-release-age-exclude[]=react-native

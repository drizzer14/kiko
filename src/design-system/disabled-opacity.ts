// The opacity a disabled control dims to. Shared by every design-system
// pressable (Button, IconButton) so a disabled action reads identically
// regardless of which primitive rendered it — never redeclare this value
// inline in a component's styles.
export const DISABLED_OPACITY = 0.4;

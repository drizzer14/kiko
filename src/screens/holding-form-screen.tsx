import type { FC } from 'react';
import { Screen } from '../design-system/components/screen';
import { Text } from '../design-system/components/text';

// Placeholder — a later task replaces this with the real Holding form screen.
export const HoldingFormScreen: FC = () => (
  <Screen>
    <Text variant="title">HoldingForm</Text>
  </Screen>
);

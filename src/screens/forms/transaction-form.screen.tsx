import type { FC } from 'react';
import { Screen } from '../../design-system/components/screen';
import { Text } from '../../design-system/components/text';

// Placeholder — a later task replaces this with the real Transaction form screen.
export const TransactionFormScreen: FC = () => (
  <Screen>
    <Text variant="title">TransactionForm</Text>
  </Screen>
);

import { StyleSheet } from 'react-native-unistyles';

export const styles = StyleSheet.create(() => ({
  // The icon and name sit on one baseline row, vertically centered so the
  // heading text aligns with the middle of the (taller) glyph.
  container: {
    alignItems: 'center',
  },
}));

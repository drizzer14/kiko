import type { FC } from 'react';
import { Screen } from '../design-system/components/screen';
import { Text } from '../design-system/components/text';

// Placeholder — a later task replaces this with the real Account form screen.
export const AccountFormScreen: FC = () => (
  <Screen>
    <Text variant="title">AccountForm</Text>
  </Screen>
);
